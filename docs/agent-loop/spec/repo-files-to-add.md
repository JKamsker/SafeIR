# 03 - Repository Files To Add

This file gives Codex an implementation checklist for repo changes.

## Root `AGENTS.md` addition

Add or merge the following section into the repository root `AGENTS.md`.

```md
## Agent Queue Workflow

This repository uses `agentq` for multi-agent audit/fix/verify work.

Rules:

- Do not manually edit generated files under `docs/agent-loop/queues/`.
- Do not manually change finding statuses in Markdown.
- Use `agentq append` to create findings.
- Use `agentq claim` before fixing a finding.
- Use `agentq fix` after implementing a fix.
- Use `agentq verify` to close/check off a finding.
- A fixer must not verify its own fix.
- Auditors must not edit production source code.
- Verifiers should not implement fixes unless explicitly assigned.
- Prefer one finding per PR/commit sequence.
- For behavior changes, add or update tests.
- For hot-path performance changes, add or update benchmarks/allocation tests where practical.
- Do not claim performance improvements without evidence.
- Run `agentq doctor` before handoff if you touched the queue.

Source of truth:

- `docs/agent-loop/findings/*.md`
- `docs/agent-loop/events/*.jsonl`

Generated views:

- `docs/agent-loop/queues/*.md`
```

## `docs/agent-loop/README.md`

Create:

```md
# Agent Loop

This folder contains the multi-agent finding queue.

Do not manually edit generated queue files.

Use `agentq`:

```bash
agentq append ...
agentq claim ...
agentq fix ...
agentq verify ...
agentq render
agentq doctor
```

Status flow:

```text
open -> claimed -> fixed_pending_verification -> verified
```

Auditors create findings.
Fixers fix one finding at a time.
Verifiers close findings.
```

## `docs/agent-loop/config.json`

Create:

```json
{
  "version": 1,
  "areas": {
    "completeness": {
      "prefix": "CMP",
      "queueFile": "completeness.md"
    },
    "correctness": {
      "prefix": "COR",
      "queueFile": "correctness.md"
    },
    "perf_alloc": {
      "prefix": "PAL",
      "queueFile": "perf-alloc.md"
    },
    "perf_algorithm": {
      "prefix": "ALG",
      "queueFile": "perf-algorithm.md"
    },
    "api_coherence": {
      "prefix": "API",
      "queueFile": "api-coherence.md"
    },
    "test_coverage": {
      "prefix": "TST",
      "queueFile": "test-coverage.md"
    }
  },
  "priorities": [
    "critical",
    "high",
    "medium",
    "low",
    "idea"
  ],
  "statuses": [
    "open",
    "claimed",
    "fixed_pending_verification",
    "verified",
    "rejected",
    "duplicate",
    "obsolete"
  ],
  "forbidSelfVerification": true,
  "queueGeneratedWarning": "<!-- GENERATED BY agentq render. DO NOT EDIT BY HAND. Source of truth: docs/agent-loop/findings/*.md -->"
}
```

## `docs/agent-loop/active/current-fix.md`

Create from `templates/CURRENT_FIX_TEMPLATE.md`.

## `.gitignore` additions

Usually do **not** ignore findings or queues. They should be versioned.

Optionally ignore only transient files:

```gitignore
docs/agent-loop/.agentq.lock
docs/agent-loop/tmp/
```

## Optional local wrapper

Create `scripts/agentq.ps1`:

```powershell
dotnet run --project tools/AgentQueue/src/AgentQueue -- @args
```

Create `scripts/agentq.sh`:

```bash
#!/usr/bin/env bash
set -euo pipefail
dotnet run --project tools/AgentQueue/src/AgentQueue -- "$@"
```

## Done

- `agentq doctor` passes.
- Root `AGENTS.md` contains the queue workflow rules.
- Generated queue files contain a generated warning.
