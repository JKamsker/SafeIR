{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/JKamsker/Safe-IR/schemas/v1/safe-ir-plugin-package.schema.json",
  "title": "Safe IR Plugin Package Envelope",
  "description": "Versioned JSON Schema for the plugin package envelope accepted by PluginPackageJsonSerializer.Import(string json). Mirrors the importer's strict shape for the manifest, embedded module, live settings, hook subscriptions, and kernel entrypoints. The 'module' property follows the Safe IR module envelope schema (safe-ir-module.schema.json). Schema version is tracked by the directory segment (v1) and the schemaVersion constant; bump both when the JSON contract changes.",
  "x-safe-ir-schema-version": "1.0.0",
  "type": "object",
  "additionalProperties": false,
  "required": ["manifest", "module"],
  "properties": {
    "manifest": { "$ref": "#/$defs/manifest" },
    "module": {
      "$ref": "https://github.com/JKamsker/Safe-IR/schemas/v1/safe-ir-module.schema.json"
    },
    "entrypoints": { "$ref": "#/$defs/entrypoints" }
  },
  "$defs": {
    "manifest": {
      "type": "object",
      "additionalProperties": false,
      "required": ["pluginId", "contract", "mode", "effects", "liveSettings", "subscriptions"],
      "properties": {
        "pluginId": { "type": "string" },
        "contract": { "type": "string" },
        "mode": {
          "type": "string",
          "description": "Parsed case-insensitively into the ExecutionMode enum."
        },
        "effects": {
          "type": "array",
          "items": { "type": "string" }
        },
        "liveSettings": {
          "type": "array",
          "items": { "$ref": "#/$defs/liveSetting" }
        },
        "subscriptions": {
          "type": "array",
          "items": { "$ref": "#/$defs/subscription" }
        },
        "requiredCapabilities": {
          "type": "array",
          "description": "Analyzer-derived capabilities the verified IR requires. Optional for back-compat.",
          "items": { "type": "string" }
        }
      }
    },
    "liveSetting": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "type", "defaultValue"],
      "properties": {
        "name": { "type": "string" },
        "type": { "type": "string" },
        "defaultValue": { "$ref": "#/$defs/liveSettingValue" },
        "min": { "$ref": "#/$defs/liveSettingValue" },
        "max": { "$ref": "#/$defs/liveSettingValue" }
      }
    },
    "liveSettingValue": {
      "description": "A JSON scalar (or null) interpreted according to the live setting 'type'.",
      "type": ["boolean", "integer", "number", "string", "null"]
    },
    "subscription": {
      "type": "object",
      "additionalProperties": false,
      "required": ["event", "kernel"],
      "properties": {
        "event": { "type": "string" },
        "kernel": { "type": "string" }
      }
    },
    "entrypoints": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "shouldHandle": { "type": "string" },
        "handle": { "type": "string" }
      }
    }
  }
}
